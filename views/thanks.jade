extends layout

block content
  h1 the sign has been updated!
  p=sign.top
  p=sign.bottom
  h2 unless something broke
  h3 in which case, you should check <a href="http://#{host}:#{port}">#{host}:#{port}</a> or <a href="http://localhost:#{port}">localhost:#{port}</a>
